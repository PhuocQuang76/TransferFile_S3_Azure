apiVersion: v1
kind: Secret
metadata:
  name: {{ include "filetransfer-chart.fullname" . }}-secret
  labels:
    {{- include "filetransfer-chart.labels" . | nindent 4 }}
type: Opaque
stringData:
  {{- range $key, $val := .Values.secret.data }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}